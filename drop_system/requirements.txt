# Core (all modes)
numpy>=1.24

# LIVE / DRY_RUN telemetry only
pymavlink>=2.4.37

# OFFLINE analysis only (Monte Carlo CSV export, calibration, plots) —
# not required for the live prediction/state-machine/servo path.
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Testing
pytest>=7.0
